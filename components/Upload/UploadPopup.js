import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const UploadPopup = () => {
  return (
    <View>
      <Text>UploadPopup</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
